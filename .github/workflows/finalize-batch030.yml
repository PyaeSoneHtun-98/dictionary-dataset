name: Finalize Dictionary Batch 030

on:
  push:
    paths:
      - 'staging/batch030/FINALIZE'

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install pronunciation helper
        run: python -m pip install --quiet eng-to-ipa
      - name: Assemble and validate Batch 030
        run: |
          python - <<'PY'
          import base64, hashlib, json, pathlib, re, zlib
          import eng_to_ipa as e2i

          root=pathlib.Path('.')
          manifest_path=root/'dictionary_manifest.json'
          lookup_path=root/'lookup'/'used_keys_current.zlib.b64'
          manifest=json.loads(manifest_path.read_text(encoding='utf-8'))
          assert manifest['latestBatch']==29 and manifest['nextBatch']==30
          assert manifest['batchCount']==29 and manifest['totalHeadwords']==14500
          assert manifest['lookup']['throughBatch']==29
          assert manifest['lookup']['uniqueLookupKeys']==26999
          assert manifest['lookup']['uniqueStoredForms']==12545

          encoded=lookup_path.read_text(encoding='ascii').strip()
          assert hashlib.sha256(encoded.encode('ascii')).hexdigest()==manifest['lookup']['sha256']
          prior=set(zlib.decompress(base64.b64decode(encoded)).decode('utf-8').splitlines())
          assert len(prior)==26999

          rows=[]
          for raw in (root/'staging/batch030/final_selection.tsv').read_text(encoding='utf-8').splitlines():
              if raw.strip():
                  w,pos,freq,phones,definition=raw.split('\t',4)
                  rows.append({'word':w.strip(),'sourcePos':pos.strip(),'phones':phones.strip(),'definition':definition.strip()})
          words=[r['word'] for r in rows]; headset=set(words); source_by={r['word']:r for r in rows}
          assert len(rows)==500 and len(headset)==500
          assert all(w.isascii() and w.isalpha() and w==w.lower() for w in words)
          assert not (headset & prior)
          assert 'egos' not in headset and 'ova' not in headset and 'bleep' in headset and 'tourney' in headset

          translations={}
          for p in sorted((root/'staging/batch030/translations').glob('*.tsv')):
              for raw in p.read_text(encoding='utf-8').splitlines():
                  if not raw.strip(): continue
                  parts=raw.split('\t',2); assert len(parts)==3,(p.name,raw)
                  w,pos,blob=(x.strip() for x in parts)
                  glosses=[x.strip() for x in blob.split('|') if x.strip()]
                  assert w not in translations,w
                  translations[w]=(pos,glosses)
          assert len(translations)==500 and set(translations)==headset
          allowed={'noun','verb','adjective','adverb','pronoun','preposition','conjunction','interjection','determiner','modal','auxiliary'}
          latin=re.compile(r'[A-Za-z]')
          for w,(pos,g) in translations.items():
              assert pos in allowed and 1<=len(g)<=3
              assert all(x and not latin.search(x) for x in g),(w,g)

          reviewed_forms={
            'rename':['renames','renamed','renaming'],
            'ret':['rets','retted','retting'],
            'hone':['hones','honed','honing'],
            'skim':['skims','skimmed','skimming'],
            'propel':['propels','propelled','propelling'],
            'diss':['disses','dissed','dissing'],
            'stoop':['stoops','stooped','stooping'],
            'innovate':['innovates','innovated','innovating'],
            'nab':['nabs','nabbed','nabbing'],
            'discontinue':['discontinues','discontinued','discontinuing'],
            'mow':['mows','mowed','mown','mowing'],
            'rouse':['rouses','roused','rousing'],
            'subtract':['subtracts','subtracted','subtracting'],
            'undercut':['undercuts','undercutting'],
            'deduct':['deducts','deducted','deducting'],
            'impart':['imparts','imparted','imparting'],
            'lug':['lugs','lugged','lugging'],
            'propagate':['propagates','propagated','propagating'],
            'redefine':['redefines','redefined','redefining'],
            'reinstate':['reinstates','reinstated','reinstating'],
            'retrograde':['retrogrades','retrograded','retrograding']
          }
          verb_words={w for w,(pos,_) in translations.items() if pos=='verb'}
          assert verb_words==set(reviewed_forms),(sorted(verb_words),sorted(reviewed_forms))
          dry=json.loads((root/'staging/batch030/ASSEMBLY_DRYRUN.json').read_text(encoding='utf-8'))
          assert dry['entries']==500 and dry['priorLookupKeys']==26999
          assert dry['verbForms']==reviewed_forms and dry['proposedFormCount']==63

          all_reviewed_forms=[f for w in words for f in reviewed_forms.get(w,[])]
          assert len(all_reviewed_forms)==63 and len(set(all_reviewed_forms))==63
          assert not (set(all_reviewed_forms)&prior) and not (set(all_reviewed_forms)&headset)
          assert all(f.isascii() and f.isalpha() and f==f.lower() for f in all_reviewed_forms)

          def fallback_ipa(phones):
              cmap={'B':'b','CH':'tʃ','D':'d','DH':'ð','F':'f','G':'ɡ','HH':'h','JH':'dʒ','K':'k','L':'l','M':'m','N':'n','NG':'ŋ','P':'p','R':'r','S':'s','SH':'ʃ','T':'t','TH':'θ','V':'v','W':'w','Y':'j','Z':'z','ZH':'ʒ'}
              vmap={'AA':'ɑ','AE':'æ','AO':'ɔ','AW':'aʊ','AY':'aɪ','EH':'ɛ','EY':'eɪ','IH':'ɪ','IY':'i','OW':'oʊ','OY':'ɔɪ','UH':'ʊ','UW':'u'}
              out=[]
              for tok in phones.split():
                  mm=re.match(r'^([A-Z]+)([012])?$',tok); assert mm,(phones,tok)
                  base,stress=mm.group(1),mm.group(2)
                  if base=='AH': sym='ə' if stress=='0' else 'ʌ'
                  elif base=='ER': sym='ɚ' if stress=='0' else 'ɝ'
                  elif base in vmap: sym=vmap[base]
                  else: sym=cmap[base]
                  if stress=='1' and base in set(vmap)|{'AH','ER'}: sym='ˈ'+sym
                  elif stress=='2' and base in set(vmap)|{'AH','ER'}: sym='ˌ'+sym
                  out.append(sym)
              return ''.join(out)

          entries=[]; fallback_words=[]
          for word in words:
              pos,glosses=translations[word]
              ipa=e2i.convert(word,stress_marks='both').strip()
              if not ipa or '*' in ipa or ipa.lower()==word.lower():
                  ipa=fallback_ipa(source_by[word]['phones']); fallback_words.append(word)
              ipa=ipa.replace('/','').strip()
              assert ipa and '*' not in ipa and '\n' not in ipa
              forms=list(reviewed_forms.get(word,[]))
              entries.append({'word':word,'pronunciation':f'/{ipa}/','forms':forms,'meanings':[{'partOfSpeech':pos,'burmese':glosses}]})

          assert len(entries)==500 and len({e['word'] for e in entries})==500
          assert fallback_words==dry['ipaFallbackWords'],(fallback_words,dry['ipaFallbackWords'])
          for e in entries:
              assert e['word'] not in e['forms'] and len(e['forms'])==len(set(e['forms']))
              assert e['pronunciation'].startswith('/') and e['pronunciation'].endswith('/') and len(e['pronunciation'])>2

          out=root/'Batches'/'dictionary_batch_030.json'
          assert not out.exists(),'Batch 030 already exists'
          out.write_text(json.dumps({'version':1,'batch':30,'entries':entries},ensure_ascii=False,indent=2)+'\n',encoding='utf-8')

          all_keys=prior|headset|set(all_reviewed_forms)
          assert len(all_keys)==26999+500+63
          plain=('\n'.join(sorted(all_keys))+'\n').encode('utf-8')
          lookup_encoded=base64.b64encode(zlib.compress(plain,9)).decode('ascii')
          lookup_path.write_text(lookup_encoded,encoding='ascii')

          manifest['latestBatch']=30; manifest['nextBatch']=31; manifest['batchCount']=30; manifest['totalHeadwords']=15000
          manifest['lookup']['throughBatch']=30
          manifest['lookup']['uniqueLookupKeys']=len(all_keys)
          manifest['lookup']['uniqueHeadwords']=15000
          manifest['lookup']['uniqueStoredForms']=12545+len(all_reviewed_forms)
          manifest['lookup']['sha256']=hashlib.sha256(lookup_encoded.encode('ascii')).hexdigest()
          manifest['batchFilenamePattern']='Batches/dictionary_batch_XXX.json'
          assert manifest['totalHeadwords']==manifest['batchCount']*500
          manifest_path.write_text(json.dumps(manifest,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')

          readme_path=root/'README.md'
          readme=readme_path.read_text(encoding='utf-8')
          old='Batches 001–029 have been generated: **14,500 unique headwords**.'
          new='Batches 001–030 have been generated: **15,000 unique headwords**.'
          assert old in readme
          readme_path.write_text(readme.replace(old,new),encoding='utf-8')

          report={'batch':30,'entries':500,'newForms':len(all_reviewed_forms),'priorLookupKeys':len(prior),'uniqueLookupKeys':len(all_keys),'totalHeadwords':15000,'nextBatch':31,'ipaFallbackCount':len(fallback_words),'ipaFallbackWords':fallback_words,'lookupSha256':manifest['lookup']['sha256'],'formsReviewed':True}
          (root/'batch030_validation.json').write_text(json.dumps(report,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          print(json.dumps(report,ensure_ascii=False,indent=2))
          PY
      - name: Commit Batch 030 atomically
        run: |
          git config user.name dictionary-dataset-bot
          git config user.email actions@users.noreply.github.com
          git add Batches/dictionary_batch_030.json lookup/used_keys_current.zlib.b64 dictionary_manifest.json README.md batch030_validation.json
          git commit -m 'Add dictionary Batch 030'
          git push
